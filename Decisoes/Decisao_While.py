numero = int(input("Digite um número: "))

while numero < 100:
    # \t = define tabulação na impreção do valor digitado pelo user
    print("\t" + str(numero))
    numero = numero + 1
print("Laço encerrado...")
